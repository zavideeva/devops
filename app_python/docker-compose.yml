version: "3.9"

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes: 
      - .:/code
    logging:
      driver: "json-file"
      options:
        max-size: "200k"
        max-file: "15"
        tag: "{{.ImageName}}|{{.Name}}"